import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    header: {
      main: "#E98F0A",
    },
    primary: {
      main: "#0076C6",
    },
    secondary: {
      main: "#545454",
    },
    warn: {
      main: "#FE0505",
    },
    background: {
      default: "#FFFFFF",
    },
  },
});

export default theme;
